
  # Build this

  This is a code bundle for Build this. The original project is available at https://www.figma.com/design/XJeePGOyxHgvLuqRb3es9h/Build-this.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  